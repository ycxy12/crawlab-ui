interface LViewsEnvironments {
  table: {
    columns: {
      key: string;
      value: string;
    };
  };
  navActions: LNavActions;
  messageBox: {
    prompt: {
      create: string;
    };
  };
}
